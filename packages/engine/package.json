{
  "name": "@archspace/engine",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "description": "Demand-driven memoized DAG execution: lanes, cancellation, retry, event stream (ARCHITECTURE \u00a77 / ADR-0007)",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./graph": "./src/graph.ts"
  },
  "scripts": {
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@archspace/node-sdk": "workspace:*",
    "@archspace/types": "workspace:*",
    "@noble/hashes": "^1.8.0"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "typescript": "^5.9.0",
    "vitest": "^3.0.0"
  }
}
